---
name: router
description: Session bootstrap and navigation hub. Read at the start of every session before any task. Contains project state, routing table, and behavioural contract.
edges:
  - target: context/architecture.md
    condition: when working on system design, integrations, or understanding how components connect
  - target: context/stack.md
    condition: when working with specific technologies, libraries, or making tech decisions
  - target: context/conventions.md
    condition: when writing new code, reviewing code, or unsure about project patterns
  - target: context/decisions.md
    condition: when making architectural choices or understanding why something is built a certain way
  - target: context/setup.md
    condition: when setting up the dev environment or running the project for the first time
  - target: patterns/INDEX.md
    condition: when starting a task — check the pattern index for a matching pattern file
last_updated: 2026-05-18
---

# Session Bootstrap

If you haven't already read `AGENTS.md`, read it now — it contains the project identity, non-negotiables, and commands.

Then read this file fully before doing anything else in this session.

## Current Project State

**Working:**

- **Wave 2 hub-file decomposition is complete as of 2026-05-18**: the former closeout items are now landed, with `src/ui/mod.rs` delegating the final `Screen::Splash` / `Screen::Monitor` input flow to `src/ui/splash_flow.rs` and `src/ui/monitor_flow.rs`, `crates/ozone-mcp/src/lib.rs` shedding both its inline test harness and its sandbox/mock-backend lifecycle cluster into `crates/ozone-mcp/src/tests.rs` and `crates/ozone-mcp/src/sandbox.rs`, and ozone+ runtime naming cleaned up from `Phase1dRuntime` to `OzonePlusRuntime`; the final Wave 2 gate passed with `make preflight`, `cargo check --workspace --all-targets --release`, a fresh release build of `ozone` / `ozone-plus` / `ozone-mcp`, and ignored `ozone-mcp` release smoke.
- **Front-door ozone-mcp fixtures now align with shipped enum spellings**: sandbox preference normalization now canonicalizes known preference enums to the persisted kebab-case form, and the release-smoke/front-door journeys use the real launcher/frontend spellings (`kobold-cpp`, `ozone-plus`, `silly-tavern`) instead of stale roadmap-era display values.
- **Wave 2 App-state encapsulation advanced in base ozone profiling transitions**: `src/ui/mod.rs` gained new profiling transition methods (`start_profile_workflow`, `reset_profile_and_open_launcher`, `open_profile_advisory`, `open_profile_failure`, `open_confirm_with_plan`, `clear_profile_success_and_open_configure_hub`, `clear_profile_failure_and_open_configure_hub`), and `src/ui/profiling_entry_flow.rs` plus `src/ui/profiling_result_flow.rs` now use those methods instead of repeated open-coded multi-field mutation clusters; tests-first coverage was added (`start_profile_workflow_sets_running_state_cluster`, `reset_profile_and_open_launcher_resets_cluster_and_screen`), default batched validation passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`, and profiling-ui validation passed in serial (`cargo test -p ozone --features profiling-ui -- --test-threads=1`)
- **Wave 2 hub-file decomposition advanced in base ozone profiling entry-state flow**: `src/ui/mod.rs` shed `Screen::ProfileAdvisory` and `Screen::ProfileConfirm` key branches into new child module `src/ui/profiling_entry_flow.rs` (`handle_profile_advisory_key`, `handle_profile_confirm_key`), moving profiling advisory action selection and confirm-to-workflow setup behind a dedicated boundary while preserving behavior; tests-first characterization coverage was added (`profile_advisory_escape_returns_to_model_picker`, `profile_confirm_escape_returns_to_configure_hub_for_saved_profile_benchmark`), default batched validation passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`, and profiling-ui validation passed in serial (`cargo test -p ozone --features profiling-ui -- --test-threads=1`)
- **Wave 2 hub-file decomposition advanced in base ozone profiling result-state flow**: `src/ui/mod.rs` shed `Screen::ProfileRunning`, `Screen::ProfileSuccess`, and `Screen::ProfileFailure` key branches into new child module `src/ui/profiling_result_flow.rs` (`handle_profile_running_key`, `handle_profile_success_key`, `handle_profile_failure_key`, `ProfilingResultOutcome`), moving profiling cancellation/success/failure transitions and advisory fallback refresh behind a dedicated boundary while preserving behavior and reducing duplicate advisory-return logic; tests-first characterization coverage was added (`profile_running_q_cancels_active_workflow`, `profile_success_escape_from_saved_profile_returns_configure_hub`), default batched validation stayed green with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`, and profiling-ui feature validation passed in serial (`cargo test -p ozone --features profiling-ui -- --test-threads=1`)
- **Wave 2 hub-file decomposition advanced in base ozone TierPicker input flow**: `src/ui/mod.rs` shed the `Screen::TierPicker` key/phase branch into new child module `src/ui/tier_picker_flow.rs` (`handle_tier_picker_key` + `TierPickerOutcome`), moving tier selection and install-phase transitions behind a dedicated flow boundary while preserving behavior; tests-first characterization coverage was added (`tier_picker_q_requests_exit_in_picking_phase`, `tier_picker_enter_on_lite_selects_launcher_without_download`)
- **Wave 2 hub-file decomposition advanced in base ozone Launcher input flow**: `src/ui/mod.rs` shed the `Screen::Launcher` key branch into new child module `src/ui/launcher_screen_flow.rs` (`handle_launcher_screen_key`), moving launcher quit/escape/navigation/enter transitions behind a focused flow boundary while preserving behavior; tests-first characterization coverage was added (`launcher_q_requests_exit`, `launcher_escape_opens_exit_confirm_screen`), and batched validation for slices 22-23 passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone ExitConfirm input flow**: `src/ui/mod.rs` shed the `Screen::ExitConfirm` key branch into new child module `src/ui/exit_confirm_flow.rs` (`handle_exit_confirm_key` + `ExitConfirmOutcome`), moving exit-confirm navigation and launcher-exit outcome routing behind a tighter boundary while preserving behavior; tests-first characterization coverage was added (`exit_confirm_enter_on_yes_requests_exit`, `exit_confirm_escape_returns_to_launcher`)
- **Wave 2 hub-file decomposition advanced in base ozone Settings-screen input flow**: `src/ui/mod.rs` shed the `Screen::Settings` key branch into new child module `src/ui/settings_screen_flow.rs` (`handle_settings_key`), moving settings section navigation, backend/frontend selection, save/discard transitions, and preferred-frontend runtime update behavior behind a dedicated flow boundary while preserving behavior; tests-first characterization coverage was added (`settings_enter_saves_selection_and_returns_launcher`, `settings_escape_discards_changes_and_returns_launcher`), and batched validation for latest slices passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone ModelPicker input flow**: `src/ui/mod.rs` shed the `Screen::ModelPicker` key branch into new child module `src/ui/model_picker_flow.rs` (`handle_model_picker_key`), moving model-filter/navigation and enter-to-launch/configure/profile transitions behind a dedicated boundary while preserving behavior; tests-first characterization coverage was added (`model_picker_escape_clears_filter_before_navigation`, `model_picker_escape_without_filter_returns_to_launcher`, `model_picker_enter_in_launch_mode_opens_configure_hub`)
- **Wave 2 hub-file decomposition advanced in base ozone Confirm input flow**: `src/ui/mod.rs` shed the `Screen::Confirm` key branch into new child module `src/ui/confirm_flow.rs` (`handle_confirm_key`), moving confirm back-navigation and launch queue transitions behind a tighter boundary while preserving behavior; tests-first characterization coverage was added (`confirm_escape_uses_back_navigation_target`, `confirm_enter_with_preferred_frontend_queues_launch_choice`), and batched regression validation for the latest slices passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone FrontendChoice input flow**: `src/ui/mod.rs` shed the `Screen::FrontendChoice` key branch into new child module `src/ui/frontend_choice_flow.rs` (`handle_frontend_choice_key`), moving frontend selection transitions behind a tighter boundary while preserving behavior; tests-first characterization coverage was added (`frontend_choice_escape_returns_to_confirm`, `frontend_choice_enter_sets_pending_launch_when_plan_exists`, `frontend_choice_down_is_clamped_to_last_option`), and validation passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone Configure Hub input flow**: `src/ui/mod.rs` shed the `Screen::ConfigureHub` key-branch into new child module `src/ui/configure_hub_flow.rs` (`handle_configure_hub_key`), moving configure-screen input transitions and override persistence behind a focused flow boundary while preserving behavior; tests-first characterization coverage was added (`configure_hub_escape_clears_state_and_returns_to_model_picker`, `configure_hub_enter_persists_override_and_moves_to_confirm`), and validation passed with `cargo test --workspace` plus `cargo clippy --workspace --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone configure-plan flow**: `src/ui/mod.rs` shed configure-plan mutation/reset helpers into new child module `src/ui/configure_plan_flow.rs`, moving manual configure context/GPU adjustment plus reset-to-recommended behavior behind a tighter boundary while preserving behavior; tests-first characterization coverage was added (`adjust_configure_plan_steps_context_size_when_context_selected`, `adjust_configure_plan_clamps_gpu_layers_when_layers_selected`, `reset_configure_plan_restores_recommended_plan`), and validation passed with `cargo test --workspace --quiet` plus `cargo clippy --workspace --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone launch execution flow**: `src/ui/mod.rs` shed the pending frontend launch execution branch into new child module `src/ui/launch_execution_flow.rs`, which now owns backend-specific launch execution and explicit launch outcomes while preserving run-loop behavior; validation passed with `cargo check -p ozone`, focused launcher regression `cargo test -p ozone settings_screen_syncs_from_saved_preferences --quiet`, and `cargo clippy -p ozone --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone configure-profile flow**: `src/ui/mod.rs` shed configure-hub profile/effective-plan helpers into `src/ui/configure_profile_flow.rs`, reducing another cohesive settings-profile seam while preserving behavior; validation passed with `cargo check -p ozone` and `cargo clippy -p ozone --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone command-overlay flow**: `src/ui/mod.rs` shed command-overlay helpers into new child module `src/ui/command_overlay_flow.rs`, moving launcher overlay state-normalization and selection logic behind a tighter boundary while preserving behavior; validation passed with `cargo check -p ozone`, focused launcher test `cargo test -p ozone settings_screen_syncs_from_saved_preferences --quiet`, and `cargo clippy -p ozone --all-targets -- -D warnings`
- **Wave 2 micro-extraction (2026-05-18)**: `handle_command_overlay_key` moved from `src/ui/mod.rs` into `src/ui/command_overlay_flow.rs`, `run_launcher_action` moved into `src/ui/launch_execution_flow.rs`, and terminal helpers `find_in_path` / `spawn_in_terminal` moved into `src/ui/launcher.rs`. Each micro-slice was validated with `cargo clippy -p ozone -- -D warnings`, `cargo test -p ozone -- --test-threads=1`, and then workspace-wide `cargo test --workspace` + `cargo clippy --workspace --all-targets -- -D warnings` — all gates passed.
- **Wave 2 hotspot reduction advanced in ozone+ runtime by externalizing inline tests**: `apps/ozone-plus/src/runtime.rs` now delegates its test module via `#[cfg(test)] mod tests;` to `apps/ozone-plus/src/runtime/tests.rs`, cutting runtime hub size materially while preserving behavior; validation passed with `cargo test -p ozone-plus --quiet` (`46` unit tests + `14` integration tests) and `cargo clippy -p ozone-plus --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in ozone-persist by externalizing inline tests**: `crates/ozone-persist/src/repository/mod.rs` now keeps only `#[cfg(test)] mod tests;` while the full repository test harness moved into `crates/ozone-persist/src/repository/tests.rs`, reducing the persistence hub-file surface without behavior changes; validation passed with `cargo test -p ozone-persist --quiet` (`49` passed) and `cargo clippy -p ozone-persist --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone catalog flow**: `src/ui/mod.rs` shed catalog refresh/report helper logic into new child module `src/ui/catalog_flow.rs`, moving catalog-selection and issue-summarization state transitions behind a tighter boundary while preserving launcher behavior; validation passed with `cargo check -p ozone`, focused launcher test `cargo test -p ozone settings_screen_syncs_from_saved_preferences --quiet`, and `cargo clippy -p ozone --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in base ozone settings flow**: `src/ui/mod.rs` shed the settings/confirm state-transition helpers `sync_settings_from_prefs`, `open_settings`, `open_exit_confirm`, and `back_from_confirm` into new child module `src/ui/settings_flow.rs`, reducing a cohesive navigation/settings seam in the base launcher hub while preserving behavior; validation passed with `cargo test -p ozone settings_screen_syncs_from_saved_preferences --quiet`, `cargo check -p ozone`, and `cargo clippy -p ozone --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in ozone-mcp with registry dispatch**: `crates/ozone-mcp/src/lib.rs` shed the central `tools/call` inline dispatch match into the new child module `crates/ozone-mcp/src/tool_dispatch.rs`, which now owns the tool-route table and unknown-tool fallback while keeping tool execution behavior unchanged; validation passed with `cargo test -p ozone-mcp --quiet` (`21` passed, `2` ignored) and `cargo clippy -p ozone-mcp --all-targets -- -D warnings`
- **Wave 2 hub-file decomposition advanced in ozone-persist with SQLite row parsing extraction**: `crates/ozone-persist/src/repository/mod.rs` shed the pure SQLite row parsing helpers `read_conversation_message`, `read_branch_record`, `parse_sqlite_text`, `parse_i64_as_u16`, `parse_i64_as_u64`, and error formatters into the new child module `crates/ozone-persist/src/repository/row_parsers.rs`, leaving repository orchestration and message/branch operations in place while moving data-conversion logic behind a tighter boundary; 6 new focused unit tests were added for type conversions and range checking, the full crate test suite passed with `cargo test -p ozone-persist` (49 total tests: 43 existing + 6 new), and `cargo clippy -p ozone-persist --all-targets -- -D warnings` stayed green after the extraction
- **Wave 2 hub-file decomposition advanced through an autonomous helper pass in ozone-persist**: `crates/ozone-persist/src/repository/mod.rs` shed the pure search-text helper `plain_text_fts_query` into `crates/ozone-persist/src/repository/search_helpers.rs`, and the pure timestamp/ID generators `current_timestamp_ms` plus `generate_uuid_like` into `crates/ozone-persist/src/repository/generated_values.rs`; both seams were moved with tests-first updates, the full `cargo test -p ozone-persist --quiet` suite passed afterward, and `cargo clippy -p ozone-persist --all-targets -- -D warnings` stayed green
- **Wave 2 hub-file decomposition advanced again in ozone-persist**: `crates/ozone-persist/src/repository/mod.rs` shed the pure repository bootstrap helpers `open_connection`, `ensure_file_with_contents`, and `secure_path` into the new child module `crates/ozone-persist/src/repository/fs_helpers.rs`, leaving repository orchestration and data-shape logic in place while moving filesystem/bootstrap setup behind a tighter boundary; the focused seam tests passed with `cargo test -p ozone-persist fs_helpers_ --quiet`, and `cargo clippy -p ozone-persist --all-targets -- -D warnings` stayed green after the extraction
- **Wave 2 hub-file decomposition advanced in ozone-persist**: `crates/ozone-persist/src/repository/mod.rs` shed the pure `PersistencePaths` and default session-artifact helpers into the new child module `crates/ozone-persist/src/repository/paths.rs`, leaving repository orchestration and database behavior in place while moving path/layout computation behind a tighter boundary; the focused seam tests passed with `cargo test -p ozone-persist persistence_paths_ --quiet`, and `cargo clippy -p ozone-persist --all-targets -- -D warnings` stayed green after the extraction
- **Wave 2 hub-file decomposition advanced in ozone-mcp**: `crates/ozone-mcp/src/lib.rs` shed the JSON-RPC framing/request-response seam into the new child module `crates/ozone-mcp/src/jsonrpc.rs`, leaving MCP server orchestration in place while moving protocol envelope parsing/writing and success/error response builders behind a tighter boundary; the focused seam tests passed with `cargo test -p ozone-mcp jsonrpc_ --quiet`, and `cargo clippy -p ozone-mcp --all-targets -- -D warnings` stayed green after the extraction
- **Wave 2 hub-file decomposition has started with a low-risk base ozone slice**: `src/ui/mod.rs` shed the pure `build_kc_args` and `build_llama_args` helpers into the new child module `src/ui/backend_args.rs`, leaving launcher state/orchestration in place while moving backend CLI flag construction behind a tiny seam; the new focused tests for KoboldCpp and llama.cpp arg generation passed with `cargo test -p ozone build_ --quiet`, `cargo check -p ozone` stayed green, and the new `.mex` pattern `hub-file-decomposition.md` records this workflow for future Wave 2 slices
- **Wave 1 contract-alignment is complete**: the top-level README, ozone+ family guide, contributor guidance, CI, release workflow, base CLI help, Cargo feature commentary, `.mex/ROUTER.md`, and `.mex/patterns/INDEX.md` now agree on dev-first integration, separate local family binaries, base-only GitHub Releases, and the `--features full` requirement for the installable base artifact; current diagnostics are clean for those touched docs/workflows
- **Wave 0 hidden-failure hardening from the audit triage plan is complete**: base ozone prefs loading now returns contextual errors for invalid or unreadable `preferences.json` while still treating missing first-run files as defaults, launcher and monitor startup surface those fallbacks explicitly, catalog bootstrap now reports missing or malformed preset/benchmark side files instead of silently flattening them into a clean empty state, splash readiness now tracks catalog bootstrap completion rather than non-empty catalog contents, `src/db.rs` no longer panics when the benchmarks DB data dir cannot be resolved, ozone+ store/bootstrap paths now propagate branch/swipe-group ID generation failures instead of using runtime `expect`, and install-update prompts now trust only the recorded `install-source-root.txt` marker rather than discovering a sync root from ambient cwd; focused loader/install tests were added and the full `make preflight` plus `cargo check --workspace --all-targets --release` validation pass stayed green
- **A repo-wide audit triage plan now exists for the next cleanup pass**: `plans/project-audit-triage-plan.md` sequences the confirmed audit findings into four execution waves: hidden-failure and trust-boundary hardening first, docs/CI/release contract alignment second, hub-file decomposition third, and protocol-debt cleanup last; the new `.mex` pattern `audit-triage-planning.md` captures this workflow for future audit-follow-up tasks
- **The second hygiene cleanup pass landed on 2026-05-18 and stayed behavior-preserving**: base ozone shed additional dead residue by removing the unused `SweepConfig.ram_total_mb` plumbing, dropping unused `BenchmarkRow` / `ProfileRow` SQLite row IDs and their dead query mapping, deleting the unused `App.last_refresh` state field, removing the dead `style_bold_green()` helper, replacing a stale profiling-completion roadmap TODO with a present-tense implementation comment, and finally deleting the stale untracked ozone+ duplicates `apps/ozone-plus/src/engine.rs` / `apps/ozone-plus/src/prefs.rs` plus the orphan `clean_app.py` helper after a reversible probe confirmed ozone+ tests and Clippy still passed without them; the same cleanup also deleted the ignored source-adjacent `graphify-out/` cache trees under `apps/ozone-plus/src/runtime/`, `crates/ozone-memory/src/`, `crates/ozone-tui/src/`, and `src/`, leaving only the repo-root shareable Graphify artifact set
- **Older app.rs refactor history docs were intentionally pruned on 2026-05-18**: the historical TUI refactor scratch plans/report were deleted after audit because they were no longer referenced by live repo docs or build/test flows; `plans/release-readiness-plan.md` remains the retained active planning document
- **The first hygiene cleanup pass landed on 2026-05-17 and stayed behavior-preserving**: root tracked build artifacts under `target/` were removed from version control while staying on disk locally, source-adjacent generated Graphify trees are now ignored (`src/**`, `crates/**`, `apps/**`) along with `tmp/graphify-scopes/` and `*.bak`, dead tracked files `apps/ozone-plus/src/hardware.rs` and `crates/ozone-tui/src/render.rs.bak` were deleted, the dead ozone+ `sysinfo` dependency was removed, `ozone-plus docs` now says the docs describe the current shipped scope, and the release workflow now passes diagnostics by exposing the packaged archive name as a step output instead of `${{ env.ARCHIVE }}`; verification passed with the new ozone+ prefs/docs regression tests, `cargo test -p ozone-plus --quiet`, `make test`, and `cargo clippy --workspace --all-targets -- -D warnings`; `apps/ozone-plus/src/engine.rs` and `apps/ozone-plus/src/prefs.rs` were intentionally left alone because they are currently untracked local files and deleting them would risk removing user work
- **A full hygiene triage was completed on 2026-05-17 and identified concrete cleanup debt before any behavior changes**: confirmed repo debt includes tracked root `target/` outputs (`target/release/ozone`, `.cargo-lock`, `.rustc_info.json`, `CACHEDIR.TAG`), a tracked backup source file at `crates/ozone-tui/src/render.rs.bak`, untracked-but-unignored source-adjacent `graphify-out/` trees under `src/` and crate `src/` directories, unwired ozone+ residue in `apps/ozone-plus/src/engine.rs`, `apps/ozone-plus/src/prefs.rs`, and `apps/ozone-plus/src/hardware.rs`, duplicate prefs logic between active and stale modules, stale user-facing ozone+ docs text (`These docs describe the future ozone+ scope.`), a live GitHub Actions diagnostic in `.github/workflows/release.yml`, and continuing runtime hygiene drift around panic paths, hard-coded timing constants, broad `#[allow(dead_code)]` suppressions, and central mega-files (`src/ui/mod.rs`, `apps/ozone-plus/src/runtime.rs`, `crates/ozone-mcp/src/lib.rs`, `crates/ozone-persist/src/repository/mod.rs`); the next cleanup pass should remove tracked artifacts and dead files first, fix ignore coverage for generated Graphify output, then tackle low-risk protocol cleanup before any structural refactors
- **Scoped Graphify analysis now has a rebuildable helper and a cleaner ozone-tui core path**: `./contrib/graphify-scope.sh --list` plus `make graphify-scope SCOPE=<scope>` now rebuild isolated graphs under `tmp/graphify-scopes/` without touching the repo-root graph, and the new `ozone-tui-core` scope sanitizes `crates/ozone-tui/src/lib.rs`, `layout.rs`, and `render/coordinator.rs` by stripping embedded `#[cfg(test)] mod tests` blocks before extraction; that reduced the TUI core view to `39` nodes, `59` edges, and `8` communities with top god nodes `build_layout_for_area()`, `run_event_loop()`, and `build_render_model()`, while the earlier broader scoped results still stand: `crates/ozone-memory/src` remains the cleanest high-signal slice (`294` nodes, `501` edges, `9` communities, only `11` inferred edges), `crates/ozone-tui/src` remains useful but test-heavy, and base `src` still needs source verification around helper names like `presets_path()`
- **Graphify developer integration is now wired into the repo workflow**: `uv tool install --upgrade --force graphifyy` plus `graphify install --platform copilot` is now the supported setup path, the root `.gitignore` now keeps shareable `graphify-out/` artifacts trackable while ignoring only local runtime files, `contrib/refresh-graphify.sh` plus `make graphify-refresh` provide a safe manual code-only refresh, and the existing repo `post-commit` / `post-merge` hooks now invoke that helper automatically when a graph already exists so Ozone does not need Graphify's separate hook installer; README, `context/setup.md`, and a new `.mex` pattern document `/graphify .` for the initial graph and `/graphify . --update` for semantic doc/image refreshes
- **P2 accessibility/layout advanced in ozone+ overlays**: help-overlay section headings, borders, backdrop, and toast surfaces in `crates/ozone-tui/src/render/overlays.rs` now route through preset-aware helpers in `crates/ozone-tui/src/theme.rs` instead of hard-coded low-contrast colors, so `HighContrast` and the other theme presets finally affect those overlay surfaces too; the new theme regressions are green, `cargo test -p ozone-tui --quiet` and `cargo clippy -p ozone-tui --all-targets -- -D warnings` passed, and the rerun `make release-gates` stayed green after the expected local install-parity resync
- **P2 front-door automation now validates shipped launcher, Settings, confirm-launch, and frontend-choice surfaces**: `crates/ozone-mcp` fresh-user release smoke now captures JSON/PNG sidecars from the exact shipped seeded base-launcher journey, the shipped base Settings path, the shipped base confirm-launch path, and the shipped base frontend-choice path, with the settings route going through the launcher quick-command overlay instead of brittle repeated arrow navigation; `screen_check_tool` now asserts all four final screens, the focused ignored release-smoke gate passed, and the rerun `make release-gates` also passed end-to-end after the expected install-parity resync
- **P2 planner parity advanced in base ozone**: `src/planner.rs` fast launch now shares GGUF topology inspection with profiling for total-layer discovery when metadata is available, so the launcher no longer seeds default launch plans from the old size heuristic when a real layer count is present; the new regression test `fast_launch_uses_metadata_layers` is green, `cargo test -p ozone --features full --quiet` and `cargo clippy -p ozone --features full --all-targets -- -D warnings` passed, and `make release-gates` passed after the expected local install-parity resync
- **P2 runtime decomposition in ozone+ is now cleanly named and effectively closed for Wave 2**: later runtime slices moved command/helper types into `apps/ozone-plus/src/runtime/types.rs`, externalized tests to `apps/ozone-plus/src/runtime/tests.rs`, kept the parent `apps/ozone-plus/src/runtime.rs` at `435` lines of struct/bootstrap/delegation glue plus the `SessionRuntime` forwarding impl, and completed the stale roadmap-era rename to `OzonePlusRuntime`
- **Release-readiness P2 is now closed and the sprint is ready to move on**: the saved release-readiness plan now records P2 as validated complete on 2026-05-12 after the ozone+ runtime decomposition passes, planner parity, accessibility/theme hardening, and shipped-artifact front-door smoke expansion; the latest rerun `make release-gates` passed end-to-end once local install parity was resynced, so any deeper runtime or TUI coordinator decomposition is now explicitly deferred rather than part of the release-hardening sprint
- **ozone+ release-polish surfaces were tightened and live-validated**: compact mode now surfaces a high-value recall/context notice in the footer, always-visible hint bars are shorter and more truthful, help now behaves like a closable overlay (`Esc/q/?`), `/memories` is the canonical recall command label while `/memory list` remains a compatibility alias, command palette + slash suggestions now rank exact/prefix/word-prefix matches and can find multi-word commands like `session retitle` from `/ret`, dead clipboard/select-all key-action placeholders were removed instead of being left as hidden TODO affordances, and a live ozone+ smoke confirmed session-open lands directly in conversation, `?` opens/closes Help, and `/mem` opens the Recall overlay with the updated close copy
- **Release smoke gates now cover shipped-artifact fresh-user and existing-user paths**: `make release-smoke` now runs two ignored `ozone-mcp` tests against `target/release` binaries, the fresh base smoke uses a temp-XDG sandbox seeded with a mock model so the base launcher can progress past splash, the ozone+ smoke validates fresh and existing-user persistence through release `ozone-plus create/send/list` commands against a temp-XDG sandbox with an auto-started mock backend, and `make release-gates` passed end-to-end after a checksum sync refreshed the installed `ozone-mcp` binary
- **Base install parity and workspace release checks now have explicit Make targets**: `./contrib/sync-local-install.sh` now supports `--verify-only` to fail on missing or stale installed binaries without rewriting them, `make verify-install-parity` exposes that check directly, `make release-gates` now bundles workspace preflight plus install parity verification, and the new gates were validated locally by first detecting a stale installed `ozone` binary and then passing after a `--no-build` sync repaired parity
- **ozone+ first-memory surfaces now distinguish pinned, note, and searchable memories**: `/memory list` and `:memories` now load combined saved-memory state instead of only pinned-message artifacts, saved-memory output labels note vs pinned entries explicitly, saved-memory removal now works for both note and pinned-message artifacts, shell/help/README copy no longer advertises a nonexistent `/memory pin <text>` command, and both `cargo test -p ozone-plus -p ozone-persist --quiet` and `cargo clippy -p ozone-plus -p ozone-persist --all-targets -- -D warnings` are green
- **ozone+ first-chat backend guidance now matches the shipped runtime**: unsupported or unreachable ozone+ chat backends now tell the user that chat generation requires KoboldCpp or llama.cpp and point them at `ozone` or a corrected session URL/config, the `ozone-plus --help` copy now states the same backend contract, the top-level README ozone+ section now gives the same first-send guidance, and both `cargo test -p ozone-plus --quiet` and `cargo clippy -p ozone-plus --all-targets -- -D warnings` are green
- **Base ozone full-feature release path is clippy-clean again**: the profiling workflow now honors the backend requested by UI prefs instead of silently auto-selecting at execution time, large profiling workflow events are boxed, dead profiling report fields were removed, benchmark result persistence now uses a compact request struct, and both `cargo clippy --release -p ozone --features full -- -D warnings` and `cargo test -p ozone --features full` are green
- **Base ozone packaging now matches the documented command surface**: `./contrib/sync-local-install.sh` now builds `ozone` with `--features full`, the GitHub release workflow now packages the same full-featured base artifact, and setup/contributor guidance now points at the explicit `cargo build --release -p ozone --features full` command when users need the installable base launcher with profiling and `model ...` commands
- **ozone-mcp Phase 1G journey-builder extraction is now complete**: base/ozone+ screen journey builders plus core mock-user/capturable-screen orchestrator logic were extracted from `crates/ozone-mcp/src/lib.rs` into `crates/ozone-mcp/src/testing/journey.rs`; the capturable-screen registry now points directly at the extracted testing builders, and package validation remains green with `cargo test -p ozone-mcp --quiet` (`20` tests)
- **ozone+ Phase 3 runtime decomposition advanced with a second safe slice**: recall/context/transcript conversion helpers moved out of `apps/ozone-plus/src/runtime.rs` into `apps/ozone-plus/src/runtime/recall_helpers.rs` without behavior changes; `cargo test -p ozone-plus --quiet` remains green (`40` + `11`), and `cargo check --workspace` is clean/warning-free
- **ozone+ Phase 3 runtime decomposition is now underway**: first low-risk extraction landed by moving shell-command parsing + tag/argument normalization helpers out of `apps/ozone-plus/src/runtime.rs` into `apps/ozone-plus/src/runtime/shell_commands.rs`; behavior is unchanged, `cargo test -p ozone-plus --quiet` remains green, and `cargo check --workspace` is warning-free after the split
- **ozone+ chat-loop launch-readiness pass landed**: the TUI now keeps runtime-supplied partial assistant text visible when a generation is cancelled, restores failed prompts for immediate retry, renders a transient `Thinking…` placeholder before the first streamed token arrives, applies full `RuntimeContextRefresh` payloads, follows the live transcript edge during streaming until the user scrolls away, resumes follow-latest when the user returns to the bottom, opens `ozone-plus open <session-id>` directly into `ScreenState::Conversation`, and uses transcript `author_kind` rather than the display label for assistant-only actions like reroll; fresh temp-XDG KoboldCpp smoke now confirms send, forced failure, retry, TUI cancel, and TUI reroll, and both `cargo test -p ozone-tui --quiet` and the focused `cargo test -p ozone-plus bootstrap_opens_directly_into_conversation_screen --quiet` are green
- **ozone+ active-character footer badge landed**: the chat footer now surfaces the active session character directly from existing `session_metadata.character_name`, so users can see which persona is loaded without opening the inspector; focused render coverage passed for compact and wide shell states after this change
- **ozone+ memories overlay landed and ozone-tui is clippy-clean again**: `:memories`, `/memories`, and `/memory list` now switch straight into `ScreenState::MemoriesOverlay`, the overlay renders from the existing `recall_browser` refresh payload instead of waiting on new runtime metadata, `Esc`/`q` closes it back to conversation, and both `cargo clippy -p ozone-tui --all-targets -- -D warnings` and `cargo test -p ozone-tui --quiet` are green after the pass
- **`/attach` command implemented**: user types `/attach <path>` (or `:attach <path>` or `/a <path>`) in the composer to read an external text file and inject it into the draft with a `[📎 filename]` marker; 10 MB size limit; tab-completion available; 6 focused tests pass
- **Workspace preflight is green again**: `make preflight` now passes after isolating config-sensitive tests from ambient `OZONE__...` and XDG env leakage; `ozone-inference` config tests use `ConfigLoader::without_env_overrides()` for deterministic default/merge assertions, and `ozone-plus` index-rebuild tests now sandbox `XDG_CONFIG_HOME` alongside `XDG_DATA_HOME`
- **Base ozone monitor smoke is now confirmed in-terminal**: `cargo run --quiet -- monitor` now renders the live monitor in a real terminal and returns cleanly to the shell on `q` with exit code `0`, confirming that the terminal-session restore guard plus the short live hardware cache resolved the crash-like monitor regression instead of only compiling cleanly
- **Nested Cargo targets are now ignored repo-wide**: the root `.gitignore` now uses `target/` instead of `/target/`, so crate-local Cargo build outputs no longer pollute the worktree during validation and changed-file review
- **Base ozone monitor hardening landed**: the live hardware sampler now uses a short cache instead of re-running full GPU/system probes on every 500ms fast tick, and the base launcher/monitor TUI entrypoints now arm terminal-session restoration before fallible setup like `Terminal::new()` or `hide_cursor()`, so raw mode plus the alternate screen are unwound even when startup fails early and the terminal no longer gets stranded in a crash-like state
- **ozone+ reroll flow landed**: conversation normal mode now advertises plain `r`, command palette and exact `/session reroll` route through the selected assistant reply, the runtime rebuilds context from the parent user turn, reroll completions preserve swipe ordinal `0` for the original reply, tip rerolls stay on the current branch, historical rerolls activate a fresh branch on completion, and the TUI consumes refresh-aware completions so rerolls do not duplicate the parent prompt; `./contrib/sync-local-install.sh` also refreshed the installed `ozone-plus` binary after this pass — ozone-plus tests now total `51` and ozone-tui tests now total `175`
- **Base ozone model picker scroll fix landed**: the launcher model picker now renders a real viewport window plus scrollbar, so models beyond the first screenful remain visible/selectable instead of disappearing below the fold — 1 new `ozone` test, total now `38`
- **ozone+ slash-command surface + character chat entry landed**: slash-popup accept and single-match tab-complete now sync the real composer textarea, command-palette shell commands either run immediately or prefill editable slash text predictably, palette `new` now creates a real fresh session, and pressing **Enter** on a highlighted character starts a fresh chat already bound to that character — 4 new `ozone-tui` tests, totals now `172` for `ozone-tui` and `48` for `ozone-plus`
- **ozone+ auto session titles landed**: new TUI-created chats still start as `New Conversation`, but the first completed assistant reply now auto-generates a deterministic title from character context plus early transcript keywords, `/session retitle` can regenerate one on demand, and the live TUI header updates immediately through the existing session refresh path — 1 new `ozone-memory` test, 2 new `ozone-plus` tests, and 1 new `ozone-tui` test
- **ozone+ transcript viewport scrolling landed**: the TUI now enables terminal mouse capture, gives the conversation pane a real visual-row scroll offset, pre-wraps long transcript rows so scrolling works inside tall wrapped messages, maps mouse-wheel plus `j`/`k` to that viewport instead of transcript selection, and keeps `↑/↓` for message selection/edit targets — 5 new `ozone-tui` tests, total now 168
- **Local installs resynced after the latest polish pass**: `./contrib/sync-local-install.sh` rebuilt the current release artifacts and refreshed the installed `ozone`, `ozone-plus`, and `ozone-mcp` binaries; installed `ozone` and `ozone-plus` now report `0.4.8-alpha+34aa439` from both `~/.cargo/bin` and `~/.local/bin`, and `ozone-mcp` was already current
- **Textarea command-surface pass is mostly shipped**: ozone+ Phase 1 now uses surface-specific `tui-textarea` behavior for composer, transcript edit, and command palette flows, while base `ozone` now has a `/` quick-command overlay backed by `tui-textarea`, filtered from typed `LauncherAction` metadata, and executable from launcher-facing screens; focused regressions brought base `ozone` tests to 37 and ozone-tui remains green at 162, while full live smoke still needs a cleaner capture than raw PTY output
- **v0.4.5-alpha shipped**: Settings crash fixes (usize underflow, out-of-bounds category index, `"Context"` → Model mapping, missing `Session` variant), side-by-side launch preference persists and drives launcher label, theme preset system (`DarkMint`/`OzoneDark`/`HighContrast`, default `#2DAF82`), and fully interactive editable settings (Toggle + Cycle entries for Appearance, Launch, Display categories) — 18 new tests; ozone-tui total now 143
- **Current version**: `0.4.8-alpha`
- **Base ozone launcher normalization shipped**: the base launcher now uses typed `LauncherActionId` metadata instead of numeric slots, launcher-facing screens share ozone+-aligned chrome and status language, Settings now presents an `Active Defaults` summary with clearer save/discard behavior, selected launcher/model rows use restrained ticker pulse feedback, and the installed `ozone` binary was resynced after the pass — 4 new `ozone` tests, total now 31
- **ozone+ message editor hardening landed**: persisted transcript edits now use a dedicated textarea surface instead of sharing normal draft history/slash behavior, multiline cursor offsets and trailing newline sync are preserved correctly, save/cancel restore the prior working draft instead of persisting the temporary edit buffer, composer copy is clearer in edit mode, and long edit buffers now surface composer scrollbar feedback — 7 new `ozone-tui` tests, total now 161
- **Configure Hub + transcript polish landed**: base `ozone` now has a manual Configure Hub with a visible **Configure Model** launcher action, per-model saved launch overrides, planner-backed context/VRAM/RAM warnings, higher context presets up to `64k`, and explicit warning escalation above `32k`, plus launch-time effective-plan application for both KoboldCpp and llama.cpp; `ozone+` now shows transcript scrollbars, supports `Ctrl+I` editing for persisted transcript rows through the real runtime edit path, and routes `Characters › Edit` through the proper menu layout instead of leaking the live session transcript behind the form
- **Configure Hub saved-profile pass shipped**: users can now save multiple named per-model launch profiles (`custom-N` slots), mark one as the default launch profile, inspect per-profile benchmark summaries in Configure Hub, load/update/delete profile configs in-place, and benchmark a selected saved profile so tokens/sec, TTFT, VRAM, and RAM reports stay attached to that exact launch config
- **ozone+ hardening + MCP sprint shipped**: version strings are aligned to `0.4.8-alpha`, ozone+ settings/character/theme regressions are fixed, and ozone-mcp front-door launcher/chat journeys now auto-provision the right sandbox/backend path for live smoke work
- **Conversation reset follow-up landed**: `New Chat` now creates and switches into a fresh persisted session instead of reusing the previous transcript, hydration clears stale draft/runtime state, and the ozone+ shell clears the frame before redraw so old transcript rows/separators no longer ghost between conversations
- **Session folders shipped and live-tested**: ozone+ session browsing now groups by `folder:*` tags, shows folder headers, supports `f` inline assignment + `F` unfile, and includes a runtime-loop fix so folder-picker actions execute even though the picker returns `KeyAction::Noop`
- **Build-artifact hygiene is now explicit**: `./contrib/prune-build-artifacts.sh` prunes rebuildable Cargo outputs (`deps`, `incremental`, `build`, `.fingerprint`, docs, `release-lite`) while keeping current top-level debug/release binaries by default, and `make prune-artifacts` / `make prune-artifacts-dry-run` wrap it
- **Post-cleanup smoke is complete**: after pruning old artifacts, the kept `target/debug` binaries still launch and MCP screenshot captures still reach the base launcher plus ozone+ main menu, sessions, and settings surfaces; no cleanup-specific runtime regression was observed in those front-door screens
- TUI QOL upgrade shipped: scrollable lists with `PgUp`/`PgDn` (ratatui `ListState` + `Scrollbar`), slash autocomplete popup, settings drill-down, 1-row footer, braille spinner during generation, message separators, colored INS/CMD mode badges, Model Intelligence screen, and side-by-side monitor mode
- llama.cpp native profiler support shipped: full QuickSweep/FullSweep/SingleBenchmark/GenerateProfiles parity with KoboldCpp, token rate from `/completion` timings, profile export to `~/.local/share/ozone/`, auto backend detection, structured launcher args saved to prefs, and classified startup diagnostics (`GgmlAbort`, `CudaOom`, `CudaError`, `ModelLoadFailed`, `MissingSharedLibrary`, `RuntimeCrash`, `Timeout`)
- Launcher dashboard, model picker, launch confirm flow, and live monitor all work in the ratatui TUI
- CLI profiling commands work: `bench`, `sweep`, `analyze`, profile generation, and preset export
- New TUI profiling flow works: advisory -> confirm -> running -> success/failure report
- Profiling now gives actionable warnings for risky plans and issue reports for invalid model paths / launcher failures
- Success reports keep the UX review-first: generate/export/launch are explicit follow-up actions, not automatic side effects
- Product-family docs now include a top-level family guide plus dedicated `ozonelite`, `ozone`, `ozone+`, and shared compatibility documents under `ozone+/`
- GitHub Actions CI is clippy-clean again, and release automation is wired so pushes to `main` can create a version tag while the actual packaged release is built from the resulting tag run
- ozone+ now has a concrete phased execution plan: preserve the current `ozone` app, add a workspace pre-phase, then build `1A` through `1G` before Phase 2 memory and later assistive/group-chat layers
- The `.mex` scaffold passes `mex check` again, and `.mex/setup.sh` / `.mex/sync.sh` now work as repo-root wrappers for `mex setup` and `mex sync`
- The user Copilot skill library now includes a validated `response-consolidation` skill, and `.mex` now has a reusable pattern for future local skill customization work
- Phase 0 is now implemented: the repo is a Cargo workspace, `crates/ozone-core` owns shared product/path helpers, and `apps/ozone-plus` exists as a bootstrap CLI target
- Phase 1A is now implemented: `crates/ozone-persist` owns the ozone+ persistence bootstrap, and `apps/ozone-plus` can create, list, and open persisted sessions
- Phase 1B is now implemented: `crates/ozone-engine` exists, `crates/ozone-persist` owns durable branch/swipe/message operations, and `apps/ozone-plus` can drive send/edit/transcript/branch/swipe flows through an engine-backed CLI
- Phase 1C is now implemented: `crates/ozone-tui` owns the ozone+ shell state/input/layout/render loop, `apps/ozone-plus open <session-id>` launches the TUI by default, `--metadata` preserves the old summary path, and drafts now persist across close/reopen
- Phase 1D is now implemented: `crates/ozone-inference` owns layered config, prompt templates, streaming decode, and the first KoboldCpp gateway, while `apps/ozone-plus` now drives a real async streaming runtime with failure/cancel handling and persisted assistant turns
- Phase 1E is now implemented: `crates/ozone-engine` owns the first `ContextPlan` / `ContextAssembler` core, `apps/ozone-plus` now bridges context preview + dry-run data, and `crates/ozone-tui` now renders a real context inspector surface with a `Ctrl+D` dry-run trigger
- Phase 1F is now implemented: `crates/ozone-persist` owns character-card import + session/transcript export, `apps/ozone-plus` has `import card` / `export session` / `export transcript` CLI commands, `crates/ozone-tui` now supports bookmark toggle (`b`), session stats, and `/session` slash commands (rename/character/tags/show)
- Phase 1G is now implemented: `src/ui/mod.rs` adds `Screen::FrontendChoice` and `FrontendMode` enum; `ozone --frontend ozonePlus` bypasses the choice screen and exec-hands-off to `ozone-plus list`; the choice screen sits between Confirm and Launch with ↑↓/Enter navigation
- Phase 2A is now implemented: `crates/ozone-memory` owns typed pinned-memory / provenance / recall models, `crates/ozone-persist` reuses `memory_artifacts` + `session_search` for pinned memory and cross-session FTS recall, `apps/ozone-plus` now has `memory` / `search` CLI commands, and `crates/ozone-tui` supports `Ctrl+K`, `/memory ...`, `/search ...`, and `:memories` with recall output in the existing inspector/status surfaces
- Phase 2B is now implemented: `crates/ozone-memory` now owns optional embedding providers, retrieval scoring, and the disk-backed `usearch` vector index manager; `crates/ozone-persist` now persists embedding artifacts in `memory_artifacts`; and `apps/ozone-plus` now supports `index rebuild`, hybrid session/global search, stale-embedding filtering, and `RetrievedMemory` context injection with explicit FTS-only fallback
- Phase 2C is now implemented (alpha → gamma): `apps/ozone-plus` now has `/summarize`, memory-recall lifecycle labels, `lifecycle inspect`, GC planning and execution commands, `events compact`, and `lifecycle disk-status`; `crates/ozone-memory` owns `StorageTierPolicy`, `ArtifactStaleness`, the disk-monitor policy/status/check result types, and `VersionCompatibilityResult`; `crates/ozone-persist` owns derived-artifact GC planning/application and events compaction; `crates/ozone-inference` has full `MemoryLifecycleConfig` under `[memory.lifecycle]`
- Phase 5 is now implemented: the launcher ships the teal brand refresh, the tier picker, and `--mode` / `--pick` family selection for ozonelite / ozone / ozone+
- Phase 5.5 is now implemented: ozone now has `model list|add|remove|info`, ozone+ copy and metadata reflect the shipped MVP state, timestamps are human-readable, and that sprint established the post-`v0.4.0-alpha` development line that later advanced through the current `v0.4.8-alpha`
- MVP launcher polish landed after live install testing: saved frontend preference is now honored when launching, and the selected `ozone+` row in the tier picker uses a higher-contrast accent so it remains readable on the dark theme
- Launcher/settings UX follow-up landed after the theme lock-in: the current teal minimalist palette is preserved, `Esc` now backs out to the previous menu instead of quitting from intermediate launcher screens, the launcher itself asks for exit confirmation, and Settings now clearly exposes backend/frontend section focus with real cancel-on-Esc behavior
- Backend visibility follow-up landed after monitor feedback: service polling now tracks Ollama explicitly, the launcher and monitor service panels show all three relevant services (`KoboldCpp`, `Ollama`, `SillyTavern`), and monitor hints now match the actual back/exit keys so backend selection is easier to confirm in the installed binary
- ozone -> ozone+ launcher handoff now opens a real ozone+ session shell instead of execing `ozone-plus list`; the hidden `ozone-plus handoff` entrypoint reuses the freshest session when possible, creates a fallback launcher session when needed, and the installed binaries under both `~/.local/bin` and `~/.cargo/bin` are now synced at `0.4.1-alpha`
- Launcher smoke-fix pass landed after live testing: the splash version label no longer duplicates `alpha`, `Launch` with saved `Ollama + ozone+` now hands off into `ozone-plus`, and the TUI now clears the frame before redraws to reduce stale-screen overlap during launcher, monitor, and profiling transitions
- Base launcher live smoke re-validated on the installed binary: `Launch` with `--no-browser --frontend sillyTavern` and Ollama reaches the Monitor screen, the profiling advisor correctly routes broken `.gguf` symlinks into a reviewable failure path, and the destructive `Clear GPU` action is best verified separately because external Ollama management may keep `:11434` listening after the UI reports success
- ozone+ MVP smoke surfaces were validated on a fresh imported character session: `open` loaded the real TUI shell, a generated assistant turn persisted, `memory pin`, `memory note`, `memory list`, and `search session` worked, and pinned/note memories reached generation context in the live shell
- Post-smoke hardening landed for the remaining ozone+ blockers: `start_kobold()` now fails fast with classified launcher/install diagnostics and supports `OZONE_KOBOLDCPP_LAUNCHER` for repaired wrapper overrides, ozone+ insert/command modes now treat `?` as literal text instead of hijacking Help, and FTS-only search now includes note-memory hits in both session and global recall flows
- Fresh-user launcher/runtime follow-up landed after the latest seam audit: splash auto-advance now honors the tier picker for unset preferences, the base launcher live-refreshes its model catalog while ozone stays open, `ozone-plus send` now uses the real Phase 1D runtime path for normal user prompts, and launcher exec handoff now targets `ozone-plus handoff --launcher-session` so the launcher reuses a dedicated `Launcher Session` instead of implicitly jumping into the freshest chat
- The launcher now rejects `Ollama + ozone+` from the guided launch path with an explicit error because the current ozone+ runtime supports KoboldCpp and llama.cpp, but not Ollama; direct ozone+ shell entry remains available, and the launcher no longer pretends that the unsupported combo is wired end-to-end
- Deep live-test follow-up landed for ozone+ swipe behavior: activating a swipe candidate after branching no longer tries to retip an unrelated active branch just because the swipe parent appears earlier in that transcript, so the old ancestry-consistency failure is gone and the CLI help now points users at `swipe list` before `swipe activate`
- Fresh temp-XDG smoke on the current release artifacts still works end-to-end for the main paths: `Launch` with saved `Ollama + SillyTavern` reaches `Monitor`, `Open ozone+` execs into `ozone-plus list`, profiling a missing-model symlink lands on the issue report instead of crashing, and a real CPU-only KoboldCpp run (`mn-12b-mag-mell-r1.gguf`) can serve ozone+ with persisted assistant turns plus memory/search flows
- Base Ozone and ozone+ now have first-class llama.cpp support: `ozone model add --hf` uses llama.cpp's built-in HF downloader and then links the resolved GGUF from the HF cache into `~/models/`; `crates/ozone-inference` now supports both `koboldcpp` and `llamacpp`; and the base launcher can now launch `LlamaCpp` directly and hand that runtime off into ozone+
- Base Ozone profiling is now layer-aware inside the profiling workflow: GGUF metadata feeds GPU/CPU split recommendations, estimated RAM, and sweep seeding, while a live Pantheon 22B profiling smoke completed advisory -> confirm -> running -> saved benchmark with a 42 GPU / 14 CPU split at ctx 2048 (`8.08 tok/s`, `10498 MiB` VRAM peak)
- User-facing docs now describe the base `Profile` workflow as **autoprofiling**: a strong benchmark-backed starting point for manual GPU/CPU layer tweaking rather than a promise of final automatic tuning
- Documentation overhauled (philosophy-first pass): README.md fully restructured with philosophy/family story leading, KoboldCpp install guide, model setup guide, first-run walkthrough, autoprofiling walkthrough, ozone+ TUI reference, CPU-only mode, env vars, troubleshooting; `ozone+/README.md` Section 10 updated to reflect shipped MVP state and TUI slash command reference added; `CONTRIBUTING.md` updated for workspace structure and ozone+ crates; `ozone_plus_documentation_stack.md` Section 6 updated to reflect what is actually still pending
- Phase 7 live-test fixes are now shipped in the base `ozone` binary: model commands use consistent `✗` errors, empty/path-like model names are rejected, broken symlinks surface as `⚠ broken` in list output, `ozone model list` supports `--json`, release builds strip symbols by default, and an `oz+` symlink now selects the ozone+ tier like `ozone+`
- First-cut Ozone MCP automation now exists as `apps/ozone-mcp` + `crates/ozone-mcp`: a stdio MCP server with repo-dev tools, ozone+ session/memory/search/branch/swipe/import/export tools, temp-XDG sandbox + mock KoboldCpp helpers, and a higher-level `launcher_smoke` tool; real protocol smokes now cover `initialize`, `tools/list`, sandboxed runtime-backed send/search, and launcher invocation flow
- Ozone MCP now also has a front-door `mock_user_tool` for terminal-only scripted user journeys: it launches the real `ozone` binary in a PTY, drives named launcher/ozone+ flows with keys/text, and reports success from recent-screen terminal markers instead of repo/API back doors
- Ozone MCP now has a centralized capturable-screen navigation catalog plus target-name-driven mock-user reuse for base Ozone and ozone+ entry screens, so later screenshot tooling can navigate from cold start without duplicating flow definitions
- Ozone MCP now has a standalone `screenshot_tool` that reuses those centralized screen targets to drive front-door PTY captures and write PNG + JSON sidecars for capturable launcher / ozone+ screens
- Ozone MCP now has `screen_check_tool`, which reads those screenshot JSON sidecars and runs structured screen-grid checks (text, color, border, layout, overlap) without OCR
- Installed release binaries were resynced after the MCP/front-door testing work: `ozone`, `ozone-plus`, and `ozone-mcp` under both `~/.local/bin` and `~/.cargo/bin` match the release artifacts from that sprint; the current development line is now `0.4.8-alpha`
- Local install sync is now checksum-aware via `./contrib/sync-local-install.sh`: it explicitly builds `ozone`, `ozone-plus`, and `ozone-mcp`, then refreshes both `~/.cargo/bin` and `~/.local/bin` only when the built SHA-256 differs from the installed copy
- Installed `ozone` and `ozone-plus` now also detect stale local installs at startup: if the synced repo's `target/release` artifact no longer matches the installed checksum, the app prompts `Update installed binaries now? [Y/n]` and can refresh/relaunch in place
- MCP screenshot artifacts were re-validated in a temp-XDG live-test pass: direct ozone+ targets still write usable PNG + JSON sidecars and `screen_check_tool` can assert against them reliably, even though the current marker-based runner can still report false failures for successful captures
- Ozone MCP front-door helpers now auto-provision target-aware temp-XDG sandboxes when `sandboxId` is omitted from `mock_user_tool` / `screenshot_tool`, and the built-in ozone+ chat journey now starts its mock KoboldCpp backend automatically instead of failing in an empty fresh sandbox

**Phase 2 is COMPLETE. All of Phase 2A, 2B, and 2C (alpha/beta/gamma) have shipped.**

**Phase 3 is COMPLETE (2026-04-14).** All cleanup tasks a–e shipped in commits db33494 and af96d0c:

- `ThinkingBlockDecoder` wired into streaming token loop; `/thinking [status|hidden|assisted|debug]`
- `ImportanceScorer` + `KeywordExtractor` wired post-generation when Tier B enabled; `/tierb [status|toggle]`
- `HooksConfig` fires pre/post generation; `/hooks [status|list]`
- Safe mode (`/safemode [status|on|off|toggle]`) gates all Tier B features
- 8 new integration tests; workspace total 218 → 226

**Not yet built:**

- TUI cancellation for in-flight profiling runs
- A dedicated in-TUI browser for existing benchmark history and Pareto data
- Rich per-substep sweep visualizations beyond streamed text progress
- Phase 3 (group chat, assistive layer, original ozone integration) — **COMPLETE, see above**
- Phase 4 (scene foundation: multi-actor scenes, speaker routing, group-chat model)

**Known issues:**

- ~~Settings crashes in ozone+ (usize underflow, out-of-bounds index, missing Session variant)~~ — **resolved in v0.4.5-alpha**
- Theme preset system is now live (`ThemePreset` enum, `ACTIVE_PRESET` OnceLock); runtime hot-swap via `Settings > Appearance` cycle entry
- Broken `.gguf` symlinks can still appear in the catalog/list, but base CLI list views now mark them as `⚠ broken` and the picker still routes them into issue reports when selected
- Hardware guidance is still NVIDIA-centric because GPU memory detection depends on `nvidia-smi`
- Focused ozone+ tests now cover core, engine, persistence, TUI, and app-level draft restore; the older root `ozone` app still has intentionally light test coverage outside profiling helpers
- Manual swipe seeding in `ozone-plus` is still a temporary CLI helper for transcript experiments; automatic alternate-generation/swipe creation is not yet wired into the runtime
- Several `~/models/*.gguf` symlinks still point at missing Ollama blob files (`Qwen2.5-0.5B.gguf`, `Qwen2.5-Coder-7B-Q5_K_M.gguf`, and others), so the launcher's profile/catalog paths can only treat them as broken-model issue reports until the model files are repaired or removed
- The Phase 1E live smoke verified inline context preview with a mock backend, but the `Ctrl+D` path itself was verified through cargo tests because this PTY automation channel cannot reliably send every control chord
- The Phase 2A live smoke verified `:memories` and `/search ...` in the TUI, but `Ctrl+K` itself was validated through tests because this PTY automation channel cannot reliably send every control chord
- The Phase 2B live smoke verified CLI fallback -> `index rebuild` -> hybrid search with the mock embedding provider; the full-screen TUI hybrid-recall path is still better covered by cargo tests than PTY-driven visual automation
- `cargo build --release` at the repo root is not sufficient when you want a fresh `target/release/ozone-plus` artifact; explicitly building `-p ozone-plus` avoids stale smoke-test binaries, and PTY/front-door debug smokes should rebuild `-p ozone -p ozone-plus -p ozone-mcp-app` (or `--workspace`) before trusting `target/debug/*`
- PTY/script transcript captures can still show raw ANSI/control-sequence noise even after the redraw hardening pass; treat that as a capture limitation unless it is also reproducible in a normal interactive terminal
- `Clear GPU` can report success while `:11434` is still listening if Ollama is supervised outside ozone or auto-restarts; validate the port/process state instead of assuming the listener is gone
- The local `~/koboldcpp/koboldcpp` install is now a source-built wrapper and works in both CUDA and CPU modes; remaining live-smoke blockers are model-file availability/validity and PTY automation limits, not the launcher binary itself.
- llama.cpp support assumes `llama-server` and `llama-cli` are installed or exposed through `OZONE_LLAMACPP_SERVER` / `OZONE_LLAMACPP_CLI`; this environment currently does not have either binary on `PATH`, so live launch/import smokes need explicit overrides or a local install first
- First-cut `ozone-mcp` still shells out through `ozone-plus` for runtime-backed `send`, `search`, and `index rebuild`, and its launcher smoke tool relies on PTY automation; no external editor/client integration test has been run yet.
- `mock_user_tool` is intentionally terminal-only and marker-based: it can prove front-door launcher/ozone+ flows reached expected recent-screen text, but PTY captures are still noisy and not suitable for pixel-perfect assertions.
- `target/` directory regrows to ~3GB after normal test/lint cycles; run `make prune-artifacts` periodically or after big build sessions to reclaim disk; this is expected Cargo behavior with multiple profiles and is not a bug
- Temp-XDG screenshot runs can lose user-site Python modules like `pyte` because the capture helper inherits the sandbox HOME; export a real-site `PYTHONPATH` or install the dependency system-wide before assuming the Rust MCP layer is broken.
- Repeated `ozone-plus handoff --launcher-session` invocations inside the same temp-XDG sandbox currently create duplicate `Launcher Session` rows instead of reusing one dedicated launcher session.
- Generic PTY launcher smoke remains marker-based and imperfect, but the fresh-user release gate now captures final launcher, Settings, confirm-launch, and frontend-choice sidecars and checks them with `screen_check_tool`; if launcher handoff still fails after auto-provisioned setup, treat it as a real launcher-path bug instead of a missing test fixture.

## Routing Table

Load the relevant file based on the current task. Always load `context/architecture.md` first if not already in context this session.

| Task type | Load |
| --------- | ---- |
| Understanding how the system works | `context/architecture.md` |
| Working with a specific technology | `context/stack.md` |
| Writing or reviewing code | `context/conventions.md` |
| Making a design decision | `context/decisions.md` |
| Setting up or running the project | `context/setup.md` |
| Any specific task | Check `patterns/INDEX.md` for a matching pattern |

## Versioning

See [`.mex/conventions/versioning.md`](.mex/conventions/versioning.md) for full rules.

**TL;DR:** Bump PATCH at named feature-set sprints. Small changes use git hash only. `0.5.x` = first beta. PATCH can exceed 9.

## Branch Workflow

| Branch | Purpose |
| ------ | ------- |
| `main` | Stable releases — tagged, installable, always green |
| `dev` | Integration — all feature branches merge here first |
| `feature/*` | Individual feature work — branch off `dev`, PR back to `dev` |
| `hotfix/*` | Urgent fixes — branch off `main`, merge to both `main` and `dev` |

**Rule:** `main` only receives merges from `dev` (via PR at release time) or hotfix branches. Feature work always goes through `dev`.

## Commands

- **Dev build**: `cargo build`
- **Release build + install both binaries**: `make install` (always run after any code change)
- **Tests**: `make test` or `cargo test --workspace`
- **Lint**: `make lint` or `cargo clippy --workspace --all-targets -- -D warnings`
- **Preflight check**: `make preflight` (runs lint + test — **run before every commit**)
- **Prune artifacts**: `make prune-artifacts` (reclaim disk when `target/` grows large)

> **Rule**: After any code change, always run `make preflight` before committing. Never push code with clippy warnings or failing tests.

## Behavioural Contract

For every task, follow this loop:

1. **CONTEXT** — Load the relevant context file(s) from the routing table above. Check `patterns/INDEX.md` for a matching pattern. If one exists, follow it. Narrate what you load: "Loading architecture context..."
2. **BUILD** — Do the work. If a pattern exists, follow its Steps. If you are about to deviate from an established pattern, say so before writing any code — state the deviation and why.
3. **VERIFY** — Load `context/conventions.md` and run the Verify Checklist item by item. State each item and whether the output passes. Do not summarise — enumerate explicitly.
4. **DEBUG** — If verification fails or something breaks, check `patterns/INDEX.md` for a debug pattern. Follow it. Fix the issue and re-run VERIFY.
5. **GROW** — After completing the task:
   - If no pattern exists for this task type, create one in `patterns/` using the format in `patterns/README.md`. Add it to `patterns/INDEX.md`. Flag it: "Created `patterns/<name>.md` from this session."
   - If a pattern exists but you deviated from it or discovered a new gotcha, update it with what you learned.
   - If any `context/` file is now out of date because of this work, update it surgically — do not rewrite entire files.
   - Update the "Current Project State" section above if the work was significant.
