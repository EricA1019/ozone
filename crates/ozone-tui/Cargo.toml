[package]
name = "ozone-tui"
version = "0.1.0"
edition = "2021"
publish = false
description = "Phase 1C TUI shell skeleton for ozone+"
license = "MIT"

[dependencies]
crossterm = "0.28"
ozone-core = { path = "../ozone-core" }
ratatui = { version = "0.29", features = ["crossterm"] }
