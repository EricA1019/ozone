[package]
name = "ozone-core"
version = "0.4.3-alpha"
edition = "2021"
publish = false
description = "Shared core foundations for the Ozone product family"
license = "MIT"

[dependencies]
directories = "5"
dirs = "5"
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
